# Copia este archivo a .env.local para desarrollo local personalizado
# Los valores por defecto están en src/environments/environment.ts

# URL del backend (sin /api al final)
API_URL=http://localhost:8000
